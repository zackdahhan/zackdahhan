def battingavg(hits, bats):
 avg = float(hits) * float(bats)

avg = float * hits / 2

 return total

hits = float(input("enter ammount of hits"))
bats = float(input("enter ammount of bats"))

avg = hits * bats / 2

print("the average is ", avg)